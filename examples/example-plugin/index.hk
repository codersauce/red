#[red::command(name = "ExampleCommand")]
fn example_command() {
    red::execute("Print", "Hello from the example Husk plugin!");
}

#[red::on("editor:ready")]
fn editor_ready(event: Json) {
    red::log("Example Husk plugin is ready");
}
